name: JDK9

on:
  push:
    branches:
      - master
    paths:
      - '.github/workflows/reusable-cask-checks.yml'
      - '.github/workflows/jdk9.yml'
      - 'Casks/zulu-jdk9.rb'
  pull_request:
    branches:
      - master
    paths:
      - '.github/workflows/reusable-cask-checks.yml'
      - '.github/workflows/jdk9.yml'
      - 'Casks/zulu-jdk9.rb'

jobs:
  check:
    uses: goooler/homebrew-zulu/.github/workflows/reusable-cask-checks.yml@master
    with:
      jdk-version: 9
